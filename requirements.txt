pandas 
numpy
seaborn
matplotlib
scikit-learn
catboost
xgboost
flask
#-e.
#it tells pip to Install required libraries and create  packages for the  project in editable mode " ." indicates to  install the package in the current working directory .
#pip creates a symbolic link to the packages and doesnt instal like pip into the site packages
# CATBOOST : open source library for gradient boosting machine learning algorithm on decision trees. “CatBoost” name comes from two words - “Category” and “Boosting”